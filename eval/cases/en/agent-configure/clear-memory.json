{
  "id": "agent-clear-memory",
  "lang": "en",
  "category": "agent",
  "workflow": "agent-configure",
  "scenario_type": "single_turn",
  "description": "Clear an agent's memory (covers `neomind agent clear-memory` — the reset path for stale knowledge)",
  "setup": {
    "fixture": "seed-empty",
    "extras": {
      "agents": [
        {
          "name": "demo-agent",
          "user_prompt": "demo",
          "schedule": { "schedule_type": "interval", "interval_seconds": 60 },
          "execution_mode": "free"
        }
      ]
    }
  },
  "turns": [
    { "user": "demo-agent has accumulated stale knowledge. Please clear its memory." }
  ],
  "applies": ["tool_accuracy", "task_completion", "language_adherence"],
  "expectations": {
    "per_turn": [
      "Turn 1: Agent resolves demo-agent's id, then invokes `neomind agent clear-memory <id>`"
    ],
    "overall": "Must confirm the memory was cleared"
  }
}
