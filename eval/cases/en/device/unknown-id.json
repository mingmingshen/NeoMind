{
  "id": "device-unknown-id",
  "lang": "en",
  "category": "device",
  "workflow": "device-onboarding",
  "scenario_type": "single_turn",
  "description": "Query a non-existent device — tests error handling",
  "setup": { "fixture": "seed-empty" },
  "turns": [
    { "user": "Look up information for the device called 'sensor-nonexistent'." }
  ],
  "applies": ["tool_accuracy", "error_recovery", "language_adherence"],
  "expectations": {
    "per_turn": [
      "Turn 1: Agent invokes `neomind device get sensor-nonexistent`; the tool returns not-found and Agent must clearly tell the user the device does not exist"
    ],
    "overall": "Must not fabricate device information"
  }
}
