{
  "id": "device-list-and-delete",
  "lang": "en",
  "category": "device",
  "workflow": "device-onboarding",
  "scenario_type": "multi_turn",
  "description": "Multi-turn: list devices, then delete one",
  "setup": { "fixture": "seed-default" },
  "turns": [
    { "user": "List all devices currently registered." },
    { "user": "Please delete camera-001." }
  ],
  "applies": ["tool_accuracy", "task_completion", "context_retention", "response_quality", "language_adherence"],
  "expectations": {
    "per_turn": [
      "Turn 1: Agent calls `neomind device list`, should return 3 devices",
      "Turn 2: Agent calls `neomind device delete camera-001`, referencing the device from the previous turn"
    ],
    "overall": "Second turn must actually execute the delete"
  },
  "state_queries": [
    { "type": "device_exists", "params": { "id": "camera-001" }, "expected": false }
  ]
}
