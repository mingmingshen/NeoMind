{
  "id": "extension-reload",
  "lang": "en",
  "category": "extension",
  "workflow": "extension-management",
  "scenario_type": "single_turn",
  "description": "Trigger a full reload of all extensions (covers `neomind extension reload`; no .nep install needed).",
  "setup": { "fixture": "seed-empty" },
  "turns": [
    { "user": "Reload all installed extensions." }
  ],
  "applies": ["tool_accuracy", "task_completion", "language_adherence"],
  "expectations": {
    "per_turn": [
      "Turn 1: Agent runs `neomind extension reload` (or POST /api/extensions/reload)"
    ],
    "overall": "Even with 0 extensions, must clearly confirm reload completion"
  }
}
