{
  "id": "rule-disable-existing",
  "lang": "en",
  "category": "rule",
  "workflow": "rule-management",
  "scenario_type": "single_turn",
  "description": "Disable an existing rule",
  "setup": { "fixture": "seed-default" },
  "turns": [
    { "user": "Disable the rule-temp-alert rule." }
  ],
  "applies": ["tool_accuracy", "task_completion", "language_adherence"],
  "expectations": {
    "per_turn": [
      "Turn 1: Agent calls `neomind rule disable rule-temp-alert`"
    ],
    "overall": "Should confirm after disabling"
  },
  "state_queries": [
    { "type": "rule_enabled", "params": { "id": "rule-temp-alert" }, "expected": false }
  ]
}
