{
  "id": "rule-update-and-delete",
  "lang": "en",
  "category": "rule",
  "workflow": "rule-management",
  "scenario_type": "multi_turn",
  "description": "Multi-turn: update rule threshold -> delete rule (covers `neomind rule update` + `rule delete`; previously uncovered).",
  "setup": { "fixture": "seed-default" },
  "turns": [
    { "user": "Change rule-temp-alert's threshold from 20 to 15." },
    { "user": "OK, finally delete this rule." }
  ],
  "applies": ["tool_accuracy", "task_completion", "context_retention", "language_adherence"],
  "expectations": {
    "per_turn": [
      "Turn 1: Agent resolves rule id then runs `neomind rule update <id>` with new condition threshold 15 (or PUT /api/rules/<id>)",
      "Turn 2: Agent runs `neomind rule delete <id>`"
    ],
    "overall": "Both turns must reuse the same id"
  }
}
