{
  "id": "rule-create-high-temp",
  "lang": "zh",
  "category": "rule",
  "workflow": "rule-management",
  "scenario_type": "single_turn",
  "description": "创建温度过高告警规则",
  "setup": { "fixture": "seed-default" },
  "turns": [
    { "user": "创建一条规则：当 sensor-001 的 temperature 超过 35 时发通知。规则名叫 high-temp-warning" }
  ],
  "applies": ["tool_accuracy", "task_completion", "response_quality", "language_adherence"],
  "expectations": {
    "per_turn": [
      "Turn 1: Agent 应该调用 `neomind rule create`，参数包含 condition: device:sensor-001:temperature > 35, actions: notify"
    ],
    "overall": "规则应该启用（enabled=true）"
  },
  "state_queries": [
    { "type": "rule_exists", "params": { "id": "high-temp-warning" }, "expected": true }
  ]
}
