{
  "id": "device-types-list",
  "lang": "zh",
  "category": "device",
  "workflow": "device-onboarding",
  "scenario_type": "single_turn",
  "description": "列出系统支持的设备类型模板（覆盖 `neomind device types list` — 设备 onboarding 关键，此前 0 覆盖）",
  "setup": { "fixture": "seed-empty" },
  "turns": [
    { "user": "现在系统里支持哪些设备类型？每种类型有哪些指标和命令？" }
  ],
  "applies": ["tool_accuracy", "task_completion", "language_adherence"],
  "expectations": {
    "per_turn": [
      "Turn 1: Agent 调用 `neomind device types list`（或 GET /api/device-types），并尝试总结指标/命令"
    ],
    "overall": "必须基于工具结果作答（空列表也是合法答案，但不能凭空猜测）"
  }
}
