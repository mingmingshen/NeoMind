{
  "id": "settings-timezone",
  "lang": "zh",
  "category": "settings",
  "workflow": "settings-management",
  "scenario_type": "single_turn",
  "description": "查询当前全局时区设置（覆盖 `neomind settings timezone` — settings API 面）",
  "setup": { "fixture": "seed-empty" },
  "turns": [
    { "user": "当前实例配置的全局时区是什么？" }
  ],
  "applies": ["tool_accuracy", "task_completion", "language_adherence"],
  "expectations": {
    "per_turn": [
      "Turn 1: Agent 调用 `neomind settings timezone`（或 GET /api/settings/timezone）"
    ],
    "overall": "必须返回实际的时区值（如 UTC 或 Asia/Shanghai）"
  }
}
