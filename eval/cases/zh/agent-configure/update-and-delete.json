{
  "id": "agent-update-and-delete",
  "lang": "zh",
  "category": "agent",
  "workflow": "agent-configure",
  "scenario_type": "multi_turn",
  "description": "多轮：更新 agent 的 user_prompt → 删除 agent（覆盖 `neomind agent update` + `agent delete`，此前均未覆盖）",
  "setup": {
    "fixture": "seed-empty",
    "extras": {
      "agents": [
        {
          "name": "demo-agent",
          "user_prompt": "old prompt",
          "schedule": { "schedule_type": "interval", "interval_seconds": 60 },
          "execution_mode": "free"
        }
      ]
    }
  },
  "turns": [
    { "user": "把 demo-agent 的 user_prompt 改成 '检查温度并告警'。" },
    { "user": "好的，最后把这个 agent 删掉。" }
  ],
  "applies": ["tool_accuracy", "task_completion", "context_retention", "language_adherence"],
  "expectations": {
    "per_turn": [
      "Turn 1: Agent 解析 demo-agent 的 id 后调用 `neomind agent update <id> --user-prompt '检查温度并告警'`（或 PUT /api/agents/<id>）",
      "Turn 2: Agent 调用 `neomind agent delete <id>`（或 DELETE /api/agents/<id>）"
    ],
    "overall": "两轮必须复用同一个 id"
  }
}
