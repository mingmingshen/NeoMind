[package]
name = "test_counter"
version = "0.1.0"
edition = "2021"
crate-type = ["cdylib"]

[lib]
name = "test_counter"
crate-type = ["cdylib"]

[dependencies]
neomind-extension-sdk = { path = "../../../neomind-extension-sdk" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[lib]
name = "test_counter"
path = "lib.rs"
crate-type = ["cdylib"]
