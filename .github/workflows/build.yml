name: Build NeoMind

on:
  push:
    branches: [main]
    tags:
      - 'v*'
  pull_request:
    branches: [main]
  workflow_dispatch:
  release:
    types: [published]

jobs:
  # Desktop app builds (Tauri)
  build-desktop:
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: 'macos-aarch64'
            os: macos-latest
            target: aarch64-apple-darwin
          - platform: 'macos-x86_64'
            os: macos-latest
            target: x86_64-apple-darwin
          - platform: 'windows-x86_64'
            os: windows-latest
            target: x86_64-pc-windows-msvc
          - platform: 'linux-x86_64'
            os: ubuntu-latest
            target: x86_64-unknown-linux-gnu
          - platform: 'linux-aarch64'
            os: ubuntu-22.04-arm
            target: aarch64-unknown-linux-gnu

    runs-on: ${{ matrix.os }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: 'web/package-lock.json'

      - name: Clean cache on version tags
        if: startsWith(github.ref, 'refs/tags/v')
        shell: bash
        run: |
          rm -rf ~/.cargo/registry/cache
          rm -rf web/src-tauri/target/release

      - name: Cache Rust dependencies
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry/
            ~/.cargo/git/
            web/src-tauri/target/
          # Include version tag in cache key to prevent using old cached builds
          key: ${{ runner.os }}-cargo-${{ matrix.target }}-${{ hashFiles('web/src-tauri/Cargo.lock') }}-${{ github.ref_name }}
          restore-keys: |
            ${{ runner.os }}-cargo-${{ matrix.target }}-${{ hashFiles('web/src-tauri/Cargo.lock') }}-
            ${{ runner.os }}-cargo-${{ matrix.target }}-
            ${{ runner.os }}-cargo-

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Install additional macOS target
        if: matrix.os == 'macos-latest'
        run: rustup target add x86_64-apple-darwin aarch64-apple-darwin

      - name: Install frontend dependencies
        working-directory: ./web
        run: npm ci

      - name: Build frontend
        working-directory: ./web
        shell: bash
        run: |
          npm run build 2>&1 | tee build.log
          ls -la dist/
          test -f dist/index.html || (echo "Frontend build failed: dist/index.html not found" && exit 1)

      - name: Check for circular dependencies
        working-directory: ./web
        shell: bash
        run: |
          if grep -qi "circular" build.log; then
            echo "::error::Circular dependencies detected in build output!"
            grep -i "circular" build.log
            exit 1
          fi
          echo "✅ No circular dependencies detected"

      - name: Install system dependencies (Ubuntu)
        if: startsWith(matrix.os, 'ubuntu')
        run: |
          sudo apt-get update
          sudo apt-get install -y libwebkit2gtk-4.1-dev \
            build-essential \
            curl \
            wget \
            file \
            libxdo-dev \
            libssl-dev \
            libayatana-appindicator3-dev \
            librsvg2-dev \
            xdg-utils

      - name: Build and bundle app
        working-directory: ./web
        run: npm run tauri build -- --target ${{ matrix.target }}
        env:
          TAURI_PRIVATE_KEY: ${{ secrets.TAURI_PRIVATE_KEY }}
          TAURI_KEY_PASSWORD: ${{ secrets.TAURI_KEY_PASSWORD }}

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: neomind-desktop-${{ matrix.platform }}
          path: web/src-tauri/target/${{ matrix.target }}/release/bundle/
          retention-days: 30
          if-no-files-found: warn

  # Server binary build with embedded frontend (Linux x64 and ARM64, macOS x64 and ARM64)
  build-server:
    name: Server Binary (${{ matrix.os }}-${{ matrix.arch }})
    strategy:
      matrix:
        include:
          - os: linux
            target: x86_64-unknown-linux-gnu
            arch: amd64
            runner: ubuntu-latest
          - os: linux
            target: aarch64-unknown-linux-gnu
            arch: arm64
            runner: ubuntu-latest
          - os: darwin
            target: x86_64-apple-darwin
            arch: amd64
            runner: macos-latest
          - os: darwin
            target: aarch64-apple-darwin
            arch: arm64
            runner: macos-latest
    runs-on: ${{ matrix.runner }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: 'web/package-lock.json'

      - name: Install frontend dependencies
        working-directory: ./web
        run: npm ci

      - name: Build frontend
        working-directory: ./web
        run: npm run build

      - name: Prepare static assets for embedding
        run: |
          mkdir -p crates/neomind-api/static
          cp -r web/dist/* crates/neomind-api/static/
          echo "Static assets prepared:"
          ls -la crates/neomind-api/static/

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Install additional macOS targets
        if: matrix.os == 'darwin'
        run: rustup target add x86_64-apple-darwin aarch64-apple-darwin

      - name: Cache Rust dependencies
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry/
            ~/.cargo/git/
            target/
          key: server-${{ matrix.target }}-${{ hashFiles('**/Cargo.lock') }}-${{ github.ref_name }}
          restore-keys: |
            server-${{ matrix.target }}-${{ hashFiles('**/Cargo.lock') }}-
            server-${{ matrix.target }}-

      # Linux cross-compilation setup
      - name: Cache cross-compilation tool
        if: matrix.os == 'linux'
        uses: actions/cache@v4
        id: cache-cross
        with:
          path: ~/.cargo/bin/cross
          key: cross-${{ runner.os }}-v1

      - name: Install cross-compilation tool
        if: matrix.os == 'linux' && steps.cache-cross.outputs.cache-hit != 'true'
        run: cargo install cross --git https://github.com/cross-rs/cross

      - name: Build server binary with embedded frontend (Linux)
        if: matrix.os == 'linux'
        run: cross build --release -p neomind-cli --target ${{ matrix.target }} --features static

      - name: Build extension runner (Linux)
        if: matrix.os == 'linux'
        run: cross build --release -p neomind-extension-runner --target ${{ matrix.target }}

      - name: Build server binary with embedded frontend (macOS)
        if: matrix.os == 'darwin'
        run: cargo build --release -p neomind-cli --target ${{ matrix.target }} --features static

      - name: Build extension runner (macOS)
        if: matrix.os == 'darwin'
        run: cargo build --release -p neomind-extension-runner --target ${{ matrix.target }}

      - name: Create release archive
        run: |
          cd target/${{ matrix.target }}/release
          tar czf ../../../neomind-server-${{ matrix.os }}-${{ matrix.arch }}.tar.gz neomind neomind-extension-runner

      - name: Upload server artifact
        uses: actions/upload-artifact@v4
        with:
          name: neomind-server-${{ matrix.os }}-${{ matrix.arch }}
          path: neomind-server-${{ matrix.os }}-${{ matrix.arch }}.tar.gz
          retention-days: 30

  release:
    needs: [build-desktop, build-server]
    runs-on: ubuntu-latest
    if: contains(github.ref, 'refs/tags/')

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Display structure of downloaded files
        run: ls -R artifacts

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          files: |
            artifacts/**/*.dmg
            artifacts/**/*.msi
            artifacts/**/*.exe
            artifacts/**/*.AppImage
            artifacts/**/*.deb
            artifacts/neomind-server-linux-amd64/*.tar.gz
            artifacts/neomind-server-linux-arm64/*.tar.gz
            artifacts/neomind-server-darwin-amd64/*.tar.gz
            artifacts/neomind-server-darwin-arm64/*.tar.gz
          draft: false
          prerelease: false
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
