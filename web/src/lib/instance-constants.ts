/**
 * Shared constants for instance management.
 *
 * Used by both instanceSlice.ts and login.tsx to ensure
 * localStorage keys stay in sync.
 */

export const INSTANCE_CACHE_KEY = 'neomind_instance_cache'
export const CURRENT_INSTANCE_KEY = 'currentInstanceId'
export const PENDING_SWITCH_KEY = 'neomind_pending_switch'
